# Anime character assets (Frieren)

Drop image files here with these EXACT names (png or jpg, square works best ~400x400+):

| File | Used for |
|------|----------|
| `frieren.png` | Member avatar — Frieren |
| `fern.png`    | Member avatar — Fern |
| `stark.png`   | Member avatar — Stark |
| `himmel.png`  | Member avatar — Himmel |
| `hero.png`    | Workspace home hero image (tall/transparent PNG looks best) |

Notes
- Filenames are referenced by `app.js` (`ANIME_CHARS` / `WH_HERO`). Keep names identical.
- Transparent PNG recommended for `hero.png` so it blends with the banner.
- If a file is missing the app just falls back (initials avatar / no hero) — nothing breaks.
- These files are your own supplied art; they are git-ignored by default is NOT set, so commit only if you have the right to.
